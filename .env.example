# Google Analytics 4 measurement ID (public env var, safe to expose client-side)
# Format: G-XXXXXXXXXX — from GA4 Admin > Data streams > Web stream details
# Leave unset to disable GA4 entirely (no gtag script is rendered).
PUBLIC_GA4_MEASUREMENT_ID=
